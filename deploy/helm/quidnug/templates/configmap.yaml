apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "quidnug.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels: {{- include "quidnug.labels" . | nindent 4 }}
data:
  config.yaml: |
{{- .Values.config | toYaml | nindent 4 }}
